#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main()
{
    int x = -3;

    // x = sqrt(x);
    // x = pow(x, 4);
    // x = round(x);
    // x = ceil(x);
    // x = floor(x);
    x = abs(x);

    printf("%d\n", x);

    return 0;
}